{
  "name": "rt20",
  "version": "1.0.0",
  "scripts": {
    "build": "node src/build.js",
    "deploy": "./deploy.sh",
    "clean": "rm -rf public/*"
  },
  "dependencies": {
    "chance": "^1.1.9",
    "fs-extra": "^11.1.1",
    "markdown-it": "^13.0.1"
  }
}